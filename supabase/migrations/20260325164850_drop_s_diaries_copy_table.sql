/*
  # Drop s_diaries_copy table

  1. Changes
    - Drop the `s_diaries_copy` table and all its data
  
  2. Notes
    - This operation will permanently delete the table and all its contents
*/

DROP TABLE IF EXISTS s_diaries_copy;
